(function(ns){'use strict';const ok=(valid,message='')=>({valid,message});ns.Validate={positive(value,label){return Number(value)>0?ok(true):ok(false,`${label} must be greater than zero`)},nonNegative(value,label){return Number(value)>=0?ok(true):ok(false,`${label} cannot be negative`)},email(value){return !value||/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value)?ok(true):ok(false,'Enter a valid email address')},timeRange(start,end){return start&&end&&start<end?ok(true):ok(false,'End time must be after start time')},references(data){return window.Store.refs(data)?ok(true):ok(false,'One or more related records are invalid')}};})(window.EMS=window.EMS||{});
